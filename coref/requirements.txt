# --- Web ---
fastapi==0.103.2
uvicorn==0.30.1
jinja2==3.1.4
python-multipart==0.0.9

# --- Coréférence SOTA : CorPipe 25 (ÚFAL, vainqueur CRAC 2025) ---
# Le script corpipe25.py est vendu dans corpipe/ (téléchargé depuis le repo UFAL).
minnt>=1.0.0
torch>=2.7.1
transformers>=4.52.4
sentencepiece
huggingface_hub
git+https://github.com/udapi/udapi-python@690281b   # lecture/écriture CoNLL-U + CorefUD
requests                                             # appel UDPipe 2 (texte -> CoNLL-U)

# --- Visualisation des dépendances UD ---
# spacy sert UNIQUEMENT au rendu displaCy des arbres. Série 3.8 : wheels
# jusqu'à Python 3.13 (la 3.5.4 recompilait blis et cassait sur 3.12/3.13).
# L'API displacy.render(..., manual=True) est inchangée.
spacy>=3.8,<3.9

# --- Épingles de compatibilité ---
# numpy 2.x OK (torch 2.7 / transformers / spacy 3.8 le supportent ; CorPipe
# n'en fait qu'un usage basique). pandas retiré : non importé par le service.
numpy<3
