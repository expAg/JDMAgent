# Dépendances pour Hugging Face Spaces (lit ce fichier automatiquement).
# Le pyproject.toml n'est PAS utilisé par HF Spaces — on liste explicitement.

# Core (couche client/factcheck/enrich)
httpx>=0.27
diskcache>=5.6
pydantic>=2.6
python-dotenv>=1.0
tenacity>=8.2

# Couche agent / outils LangChain (utilisée par l'onglet Agent et l'extraction de claims)
langchain>=0.3
langchain-core>=0.3
langgraph>=0.2

# Providers LLM activés (routés par préfixe du modèle dans app.py)
# - langchain-anthropic    : BYOK (utilisateur fournit sa clé sk-ant-...)
# - langchain-openai       : BYOK GPT + endpoint OpenAI-compat (Gemini 2.x)
# - langchain-google-genai : SDK natif Google pour Gemini 3.x preview, qui
#                            préserve les `thought_signature` entre tours
#                            (l'endpoint OpenAI-compat ne le fait pas — cf.
#                            issue LangChain #34056)
langchain-anthropic>=0.2
langchain-openai>=0.2
langchain-google-genai>=2.0

# UI Gradio (LEGACY — sera retire en fin de migration FastAPI).
# Garde tant que app.py n'est pas supprime, pour ne pas casser le
# deploiement Gradio actuel en parallele.
gradio>=5,<6
pandas>=2.0

# Migration FastAPI (Phase fastapi-migration) — frontend statique HTML
# servi par uvicorn, endpoints REST + SSE pour stream agent/jarvis.
# Voir design_handoff_jdm_fastapi/README.md.
fastapi>=0.115
uvicorn[standard]>=0.30
sse-starlette>=2.1
python-multipart>=0.0.9

# GRASP-IT — classifieur de génitifs « A de B » (onglet Génitifs). Le modèle
# sérialisé models/grasp_it.joblib est construit avec scikit-learn 1.8.
scikit-learn>=1.6
joblib>=1.3
